/**
 * TAILWIND CSS v4 NOTE
 * ──────────────────────────────────────────────────────────────────────────
 * This project uses Tailwind CSS v4 which has moved ALL configuration
 * into CSS using the @theme directive in globals.css.
 *
 * The traditional tailwind.config.ts / tailwind.config.js file is NOT used
 * or read by Tailwind v4. All design tokens — colors, fonts, spacing — are
 * declared in app/globals.css under the @theme block.
 *
 * Design tokens defined in globals.css @theme:
 *
 * Colors:
 *   --color-ground:  #FAF8F5   → bg-ground / text-ground
 *   --color-cream:   #F5F0E8   → bg-cream  / text-cream
 *   --color-sky:     #C8D8E4   → bg-sky    / text-sky
 *   --color-ash:     #9A9A9A   → bg-ash    / text-ash
 *   --color-void:    #0A0A0A   → bg-void   / text-void
 *   --color-rupture: #C41E1E   → bg-rupture / text-rupture
 *   --color-dust:    #D4C17A   → bg-dust   / text-dust
 *   --color-frost:   #E8EEF0   → bg-frost  / text-frost
 *
 * Font families:
 *   --font-serif: 'Cormorant Garamond', Georgia, serif
 *   --font-sans:  'DM Sans', 'Helvetica Neue', sans-serif
 *
 * See: https://tailwindcss.com/docs/v4-beta#css-first-configuration
 * ──────────────────────────────────────────────────────────────────────────
 */

// No export required — Tailwind v4 does not read this file.
export {};
